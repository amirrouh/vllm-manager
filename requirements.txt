# VLLM Manager Dependencies
# Core ML and serving framework
vllm>=0.2.0

# System monitoring and process management
psutil>=5.9.0

# HTTP client for health checks
requests>=2.28.0

# Async support
aiohttp>=3.8.0

# Data validation and parsing (for config management)
pydantic>=2.0.0

# Terminal UI support (curses is built-in but we might need additional utils)
# Note: curses is part of Python standard library on Linux

# Optional: GPU monitoring (if nvidia-ml-py is needed for advanced GPU stats)
# nvidia-ml-py>=12.535.108

# Development and testing (optional, uncomment if needed)
# pytest>=7.0.0
# pytest-asyncio>=0.21.0